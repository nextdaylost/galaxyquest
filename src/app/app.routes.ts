import { Routes } from '@angular/router';

import { StackedLayoutComponent } from '@app/layouts/stacked-layout/stacked-layout.component';

export const routes: Routes = [{ path: '', component: StackedLayoutComponent }];
